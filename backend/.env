PORT=3000
CLIENT_ORIGIN=http://localhost:3001

# Where do you get the key from?
# You generate it yourself.
# Run this in your terminal:
# openssl rand -hex 32
JWT_SECRET=6981a2f5f9586af7befe04c7ae06ac43f79ee72536c41fd8beaa5ec528d1d652
JWT_EXPIRES_IN=1h

PGHOST=127.0.0.1
PGPORT=5432
PGUSER=richelsantiago
PGPASSWORD=postgres
PGDATABASE=clinic_backend

# Or use a single connection string instead of the PG* values above.
# DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/clinic_backend

# Optional when your Postgres provider requires SSL.
# PGSSLMODE=require

# Optional bootstrap values for creating the first clinic admin.
# Use with: npm run seed-admin
SEED_CLINIC_NAME="Sunrise Family Clinic"
SEED_CLINIC_SLUG="sunrise-family-clinic"
SEED_ADMIN_EMAIL="admin@sunrise.test"
SEED_ADMIN_PASSWORD="Password123!"
SEED_ADMIN_FIRST_NAME="Alicia"
SEED_ADMIN_LAST_NAME="Admin"
